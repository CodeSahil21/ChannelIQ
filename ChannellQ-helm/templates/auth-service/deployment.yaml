{{- if .Values.services.auth.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: {{ include "channellq.namespace" . }}
  labels:
    {{- include "channellq.labels" . | nindent 4 }}
    app: auth-service
spec:
  replicas: {{ .Values.services.auth.replicas }}
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
        {{- include "channellq.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: auth-service
          image: {{ .Values.global.imageRegistry }}/{{ .Values.services.auth.image }}:{{ .Values.services.auth.tag }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.services.auth.port }}
          envFrom:
            - configMapRef:
                name: {{ include "channellq.fullname" . }}-config
          env:
            - name: PORT
              value: {{ .Values.services.auth.port | quote }}
            {{- include "channellq.commonEnv" . | nindent 12 }}
            - name: FRONTEND_URLS
              value: {{ .Values.config.FRONTEND_URLS | quote }}
            - name: KAFKA_CLIENT_ID
              value: "auth-service"
            - name: KAFKA_CONSUMER_GROUP_ID
              value: "auth-service-group"
            - name: KAFKA_CREATE_TOPICS
              value: "true"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "channellq.fullname" . }}-secrets
                  key: DATABASE_URL_AUTH
          volumeMounts:
            - name: kafka-ca
              mountPath: /app/certs
              readOnly: true
          resources:
            {{- toYaml .Values.services.auth.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: {{ .Values.services.auth.healthCheck.path }}
              port: {{ .Values.services.auth.port }}
            initialDelaySeconds: {{ .Values.services.auth.healthCheck.initialDelaySeconds }}
            periodSeconds: {{ .Values.services.auth.healthCheck.periodSeconds }}
          readinessProbe:
            httpGet:
              path: {{ .Values.services.auth.healthCheck.path }}
              port: {{ .Values.services.auth.port }}
            initialDelaySeconds: 15
            periodSeconds: 5
      volumes:
        - name: kafka-ca
          secret:
            secretName: kafka-ca-cert
{{- end }}