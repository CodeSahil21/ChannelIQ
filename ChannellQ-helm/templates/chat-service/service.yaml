{{- if .Values.services.chat.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: {{ include "channellq.namespace" . }}
  labels:
    {{- include "channellq.labels" . | nindent 4 }}
    app: chat-service
  {{- if .Values.monitoring.prometheus.enabled }}
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: {{ .Values.services.chat.port | quote }}
    prometheus.io/path: {{ .Values.monitoring.prometheus.metricsPath | quote }}
  {{- end }}
spec:
  selector:
    app: chat-service
  ports:
    - port: {{ .Values.services.chat.port }}
      targetPort: {{ .Values.services.chat.port }}
      protocol: TCP
      name: http
  type: ClusterIP
{{- end }}