{{- if .Values.services.media.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: media-service
  namespace: {{ include "channellq.namespace" . }}
  labels:
    {{- include "channellq.labels" . | nindent 4 }}
    app: media-service
  {{- if .Values.monitoring.prometheus.enabled }}
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: {{ .Values.services.media.port | quote }}
    prometheus.io/path: {{ .Values.monitoring.prometheus.metricsPath | quote }}
  {{- end }}
spec:
  selector:
    app: media-service
  ports:
    - port: {{ .Values.services.media.port }}
      targetPort: {{ .Values.services.media.port }}
      protocol: TCP
      name: http
  type: ClusterIP
{{- end }}